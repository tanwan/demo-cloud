spring:
   application:
      name: demo-service-server

eureka:
   client:
      serviceUrl:
         defaultZone: http://127.0.0.1:8761/eureka/
server:
   port: 28090
management:
   endpoints:
      web:
         exposure:
            include: '*'