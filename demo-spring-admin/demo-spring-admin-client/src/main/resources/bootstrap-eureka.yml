spring:
  application:
    name: springboot-admin-client

server:
  port: 18080

# spring-admin需要配置这些
management:
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
    enabled: true